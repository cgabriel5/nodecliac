### Install Grammar Package (Atom)

_Package requires a manual install for now._

Clone the repo and copy the package (`resources/editors/atom/language-acmap`) to Atom's packages folder:

- **Windows**:

  - `%USERPROFILE%\.atom\packages`

- **Linux/macOS**:

  - `$HOME/.atom/packages`
